1. Execute the Database script "db_diccionario.sql".

2. Import the Spring Boot project via gradle.

3. Configure the "application.properties" file with the user and password of the Database.

4. Run the project.

5. Send a POST request with string list in the body with a JSON-formatted. 
   This list will be the letters you want to combine.